/**
 * Query Module
 *
 * TanStack Query (React Query) configuration and utilities.
 */

export { createQueryClient, getQueryClient } from './query-client';
export { queryKeys, roleKeys, permissionKeys, userKeys } from './query-keys';
